# Serverless Health Check API

A `/health` endpoint on AWS — Terraform IaC, Python Lambda, DynamoDB, delivered
by a GitHub Actions CI/CD pipeline for `staging` and `prod`.

Full documentation is built up across the commit history; see the final
`README.md` and `docs/adr/` for design decisions.
